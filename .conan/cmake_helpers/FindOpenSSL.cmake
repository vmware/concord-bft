find_path(OPENSSL_INCLUDE_DIR openssl/aes.h ${CONAN_INCLUDE_DIRS_OPENSSL})
find_library(OPENSSL_LIBRARY NAMES ${CONAN_LIBS_OPENSSL} PATHS ${CONAN_LIB_DIRS_OPENSSL})

include (FindPackageHandleStandardArgs)
find_package_handle_standard_args(OPENSSL DEFAULT_MSG OPENSSL_INCLUDE_DIR OPENSSL_LIBRARY)
message(FATAL_ERROR ${OPENSSL_FOUND})
if(OPENSSL_FOUND)
    set(OPENSSL_LIBRARIES ${OPENSSL_LIBRARY})
    set(OPENSSL_INCLUDE_DIRS ${OPENSSL_INCLUDE_DIR})
endif()
